# CompactHash
A minimalist, high-performance 128-bit streaming hash function written in modern C++.

CompactHashStreaming is engineered specifically for modern superscalar CPU architectures. By decoupling the block-absorption loop from the finalization phase, it maximizes Instruction-Level Parallelism (ILP) to achieve speeds exceeding 10 GiB/sec, while passing 100% of SMHasher's adversarial quality, collision, and differential test suites.

## 🚀 Key Features

High Speed: Achieves ~3.55 bytes/cycle (~10.1 GiB/sec @ 3 GHz) on bulk data.

True Streaming Architecture: Processes fragmented streams cleanly via a robust internal 16-byte buffering mechanism.

Strict Alignment Freedom: Leverages modern CPU microarchitecture capabilities with practically zero unaligned memory access penalties.

Hardware-Optimized Parallelism: Uses two entirely independent 64-bit lanes driven by parallel Weyl Sequences to keep the CPU's execution pipelines wide open.

SMHasher Certified: Zero collisions or structural biases across billions of keys in Sparse, Cyclic, TwoBytes, and Windowed test suites.

## 🛠️ Design & Architecture

Unlike traditional non-cryptographic hash functions that bottleneck the inner loop with frequent cross-lane mixing, CompactHashStreaming operates on a strict "Defer to Finalizer" philosophy:

The Inner Loop (Absorption): Data is injected linearly into two separate 64-bit states (state[0] and state[1]). Each state is incremented by its own unique, independent, odd-numbered Weyl Sequence constant. Because there are no cross-lane data dependencies, a modern out-of-order CPU can process both lanes simultaneously.

The Finalizer (Diffusion): Heavy mixing is deferred entirely to the finalize() call. It utilizes a sequential cascade cross-mix using 128-bit hardware multiplication primitives (_umul128/__int128). state[0] mutates itself, and that newly scrambled state is immediately thrown into state[1]. This strict dependency chain completely atomizes any linear structural patterns before outputting the 128-bit result.

## 📊 Performance & SMHasher Results

Below are the actual benchmark logs generated by the industry-standard SMHasher suite:

### Bulk Speed & Alignment Tests (262,144-byte keys)

    Alignment  0 -  3.559 bytes/cycle - 10182.50 MiB/sec @ 3 ghz
    Alignment  1 -  3.520 bytes/cycle - 10071.29 MiB/sec @ 3 ghz
    Alignment  4 -  3.556 bytes/cycle - 10175.06 MiB/sec @ 3 ghz
    Alignment  7 -  3.478 bytes/cycle -  9949.23 MiB/sec @ 3 ghz

### Small Key Latency

16-byte keys (Optimal block size): 41.33 cycles/hash (Direct loop path)

Streaming/Tail-buffered average: 45 - 60 cycles/hash

### Quality & Collision Test Highlights

Differential Tests: 0 total collisions out of 11,017,632,000 total tests (up-to-4-bit variations in 128-bit keys).

Keyset 'Sparse' (2048-bit keys): Passed with 0 collisions and a pure-noise statistical worst-bias window of 0.112% (proving long-range internal state stability across 128 loop cycles).

Keyset 'TwoBytes' Scale-Down: * 652k keys: 0.132% bias

86.5M keys: 0.003% bias (perfect convergence matching the Law of Large Numbers).

Keyset 'Cyclic': Passed with 0 collisions and zero cyclic resonance anomalies.

## 💻 Usage Example

CompactHashStreaming follows a standard object-oriented streaming API (init, update, finalize).

    #include "CompactHashStreaming.h"
    #include <iostream>
    #include <iomanip>
    
    int main() {
        uint64_t seed = 42;
        CompactHashStreaming hasher(seed);
    
        // Stream data in arbitrary chunk sizes
        hasher.update(reinterpret_cast<const uint8_t*>("Hello "), 6);
        hasher.update(reinterpret_cast<const uint8_t*>("World!"), 6);
    
        // Finalize and extract the 128-bit hash
        std::array<uint64_t, 2> hash = hasher.finalize();
    
        std::cout << "Hash: " << std::hex << std::setfill('0')
                  << std::setw(16) << hash[0] 
                  << std::setw(16) << hash[1] << std::endl;
    
        return 0;
    }

## ⚙️ Compilation

For maximum throughput, ensure you compile with optimization flags (-O3) and enable your platform's native 128-bit multiplication optimization primitives:

    GCC / Clang (Linux/macOS)
      Bash
      g++ -O3 -march=native main.cpp -o benchmark
      
    MSVC (Windows)
      Ensure that _umul128 is supported (native on x64).

DOS

    cl /O2 /Oi main.cpp

## 📜 License

This project is licensed under the MIT License - see the LICENSE file for details.

